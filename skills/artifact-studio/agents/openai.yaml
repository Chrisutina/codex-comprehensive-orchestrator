interface:
  display_name: "Artifact Studio"
  short_description: "Create and verify documents, slides, and spreadsheets."
  brand_color: "#0891B2"
  default_prompt: "Use $artifact-studio to complete this task with evidence and verification."
policy:
  allow_implicit_invocation: true
